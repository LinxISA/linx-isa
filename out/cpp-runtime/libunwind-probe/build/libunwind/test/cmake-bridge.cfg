## Autogenerated by libunwind configuration.
# Do not edit!

# Lit parameters serialized here for llvm-lit to pick them up
config.compiler = "/Users/zhoubot/llvm-project/build-linxisa-clang/bin/clang++"
config.enable_experimental = False
config.target_triple = "linx64-unknown-linux-musl"


#
# This file performs the bridge between the CMake configuration and the Lit
# configuration files by setting up the LitConfig object and various Lit
# substitutions from CMake variables.
#
# Individual configuration files can take advantage of this bridge by
# loading the file and then setting up the remaining Lit substitutions.
#

import os, site
site.addsitedir(os.path.join('/Users/zhoubot/linx-isa/compiler/llvm/libunwind/../libcxx', 'utils'))
import libcxx.test.format
from lit.util import which

# Basic configuration of the test suite
config.name = os.path.basename('/Users/zhoubot/linx-isa/compiler/llvm/libunwind/test/configs/llvm-libunwind-static.cfg.in')
config.test_source_root = os.path.join('/Users/zhoubot/linx-isa/compiler/llvm/libunwind', 'test')
config.test_format = libcxx.test.format.CxxStandardLibraryTest()
config.recursiveExpansionLimit = 10
config.test_exec_root = os.path.join('/Users/zhoubot/linx-isa/out/cpp-runtime/libunwind-probe/build/libunwind', 'test')

# Add a few features that are common to all the configurations
if False:
    config.available_features.add('libunwind-arm-ehabi')
if not False:
    config.available_features.add('libunwind-no-threads')

# Add substitutions for bootstrapping the test suite configuration
config.substitutions.append(('%{install-prefix}', '/Users/zhoubot/linx-isa/out/cpp-runtime/libunwind-probe/build/libunwind/test-suite-install'))
config.substitutions.append(('%{include}', '/Users/zhoubot/linx-isa/out/cpp-runtime/libunwind-probe/build/libunwind/test-suite-install/include'))
config.substitutions.append(('%{lib}', '/Users/zhoubot/linx-isa/out/cpp-runtime/libunwind-probe/build/libunwind/test-suite-install/lib'))
config.substitutions.append(('%{benchmark_flags}', ''))

# Check for objcopy tools
objcopy_path = which('llvm-objcopy', '/Users/zhoubot/llvm-project/build-linxisa-clang/bin')
if not objcopy_path:
    objcopy_path = which('llvm-objcopy')
if not objcopy_path:
    objcopy_path = which('objcopy')
if objcopy_path:
    config.substitutions.append(('%{objcopy}', objcopy_path))
    config.available_features.add('objcopy-available')
