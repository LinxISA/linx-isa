/*
 * The translation corpus itself is generated from the canonical ISA spec.
 * Keep a real suite source so --all-suites can compose that generated corpus
 * with the other compile-only carriers without referring to a missing file.
 */
void linx_translation_corpus_stub(void) {}
