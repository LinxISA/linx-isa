# ISA 与 QEMU 解码器/源码映射快照

- 生成时间（UTC）：`2026-08-16 19:27:52Z`
- 证据级别：`L1`
- 声明：`decoder_source_mapping`
- L2 运行时执行：`unavailable`
- L3 语义判定：`unavailable`
- 限制：本报告只证明规范形式能够映射到 QEMU 解码器与实现源码，不证明指令已经执行，也不证明执行结果符合体系结构语义。
- 规范唯一助记符：`731`
- QEMU 唯一解码助记符（不含内部项）：`849`
- QEMU 已映射的规范助记符：`731`
- L1 助记符映射：`731/731`（`100.0%`）
- 规范合法形式：`765`
- QEMU 已映射的规范形式：`765`
- L1 形式映射：`765/765`（`100.0%`）
- 缺失的规范助记符：`0`
- 缺失的规范形式：`0`
- 保留的规范形式：`1`
- 未映射的 QEMU 助记符：`87`

## 证据边界

本页是中文 Pages 入口使用的当前快照。完整的逐前缀统计由英文生成视图和机器可读 JSON 保存：

- [英文完整报告](https://github.com/LinxISA/linx-isa/blob/main/docs/bringup/gates/qemu_isa_coverage_latest.md)
- [机器可读报告](https://github.com/LinxISA/linx-isa/blob/main/docs/bringup/gates/qemu_isa_coverage_latest.json)

发布检查必须读取 JSON；Markdown 页面只是便于阅读的视图。L2 可执行覆盖与 L3 语义结果必须由独立运行时证据提供，不能从本 L1 映射结果推导。
